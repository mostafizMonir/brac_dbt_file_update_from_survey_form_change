create a table in the postgres db named warehouse_dbt_files_survey_form_mapping
 columns : id varchar , survey_form_id varchar, warehouse_dbt_file_name varchar can be null, settings
 
 save the script into a sql file and add this to the repo. 
 
modify code, rename get_table_name_from_survey_form method to get_dbt_file_name_from_survey_form.
inside get_dbt_file_name_from_survey_form method 
find warehouse_dbt_file_name from newly created warehouse_dbt_files_survey_form_mapping table with  survey_form_id.
if exists then return if not then use existing code  query = "SELECT table_name FROM survey_form WHERE id = %s"
